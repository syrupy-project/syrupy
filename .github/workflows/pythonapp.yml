name: Syrupy CICD

on: [push]

env:
  CI: 1

jobs:
  test:
    name: Test
    runs-on: ubuntu-18.04
    steps:
    - name: Install python dev dependencies.
      run: |
        python -m pip install pip-tools tox
    - name: Lint and Test with Tox
      run: |
        python -m tox
